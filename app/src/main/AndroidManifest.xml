<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.booknest.campusridenest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CampusRideNest">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.OfferCreateActivity"
            android:exported="false" />

        <activity
            android:name=".ui.RequestCreateActivity"
            android:exported="false" />

        <activity
            android:name=".ui.posts.PostsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.posts.PostDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.posts.EditPostActivity"
            android:label="Edit Post"
            android:parentActivityName=".ui.posts.PostDetailActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>

        <activity
            android:name=".ui.profile.ProfileActivity"
            android:label="User Profile"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>

    </application>

</manifest>