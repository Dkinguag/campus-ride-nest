<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#041E42">

    <!-- Logo Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Logo Icon Placeholder -->
        <!-- You can replace this with an ImageView showing your logo -->
        <TextView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@android:drawable/ic_dialog_map"
            android:backgroundTint="#FFFFFF"
            android:contentDescription="Campus Ride Nest Logo" />

        <!-- App Name -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Campus"
            android:textColor="#FFFFFF"
            android:textSize="42sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Ride Nest"
            android:textColor="#FFFFFF"
            android:textSize="42sp"
            android:textStyle="bold" />

        <!-- Accent Line -->
        <View
            android:layout_width="200dp"
            android:layout_height="3dp"
            android:layout_marginTop="16dp"
            android:background="#FFC72C" />

        <!-- Tagline -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:alpha="0.8"
            android:text="Safe Rides for Penn State Students"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <!-- Loading Indicator -->
        <ProgressBar
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="48dp"
            android:indeterminateTint="#FFFFFF" />

    </LinearLayout>

</RelativeLayout>
